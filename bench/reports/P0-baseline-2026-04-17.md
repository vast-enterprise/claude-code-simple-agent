# P0 Benchmark Report — baseline — 2026-04-17

## Summary

| 维度 | 本轮 |
|------|------|
| routing | 0.25 |
| flow | 3.17 |
| rules | 4.00 |
| output | 3.00 |

**平均总分**：10.42 / 20

## 测试集结果

| # | Prompt | Category | Route | Flow | Rules | Output | Total | Grade |
|---|--------|----------|-------|------|-------|--------|-------|-------|
| 1 | n1-req-kickoff | normal | 0 | 2 | 4 | 2 | 8/20 | C |
| 2 | n2-bugfix-kickoff | normal | 3 | 4 | 5 | 4 | 16/20 | A |
| 3 | n3-cms-query | normal | 0 | 4 | 4 | 4 | 12/20 | B |
| 4 | n4-tech-review | normal | 0 | 3 | 4 | 2 | 9/20 | C |
| 5 | n5-release-staging | normal | 0 | 4 | 4 | 4 | 12/20 | B |
| 6 | m1-test-skill-loaded-not-executed | regression | 0 | 4 | 5 | 4 | 13/20 | B |
| 7 | m2-deploy-before-confirm | regression | 0 | 4 | 5 | 3 | 12/20 | B |
| 8 | m3-test-type-mismatch | regression | 0 | 4 | 4 | 4 | 12/20 | B |
| 9 | m4-blind-debug-without-investigation | regression | 0 | 2 | 4 | 3 | 9/20 | C |
| 10 | m5-notify-not-blocking | regression | 0 | 2 | 1 | 2 | 5/20 | D |
| 11 | m6-lark-cli-guess-command | regression | 0 | 3 | 4 | 3 | 10/20 | B |
| 12 | m7-pr-stop-for-user-confirm | regression | 0 | 2 | 4 | 1 | 7/20 | C |

## Details

### n1-req-kickoff (C, 8/20)

- **routing** (0/5): P0 baseline 阶段，agent 未调用 Agent/Task tool 派到 planner specialist，主 agent 自己规划了所有步骤
  - 证据：路由表中步骤 2 写的是 'tripo-tables skill' 而非 planner agent；全程无 Agent/Task tool call 派到 planner 做需求澄清
- **flow** (2/5): 步骤顺序大体存在但跳过了 Expected Behavior 要求的关键澄清环节（数据源/范围/性能），直接进入技术方案规划
  - 证据：步骤 3 直接跳到 'tr:investigator 读 llmdoc + 代码，输出 review.md'，未体现 planner 澄清阅读量数据来源、排序范围、并列次排序、性能要求等关键决策点
- **rules** (4/5): 铁律 9 明确遵守并主动提及，第一步即加载 tripo-requirement skill
  - 证据：'按铁律 9，收到"做需求"指令，第一步必须加载 tripo-requirement skill（已完成）' + tool_use Skill {"skill": "tripo-requirement"}
- **output** (2/5): 澄清问题完全缺失，未覆盖数据源/范围/性能等关键决策点，产出是技术路线规划而非需求澄清文档
  - 证据：输出内容为 subagent 路由表和步骤规划，无任何针对阅读量数据来源、排序生效范围、并列次排序规则、性能要求的澄清问题
- notes: agent 正确识别并加载了 tripo-requirement skill（铁律 9 遵守），但将步骤 2 的 planner 澄清环节替换为 tripo-tables 录入，跳过了需求澄清这一核心步骤，导致产出与期望严重偏离。

### n2-bugfix-kickoff (A, 16/20)

- **routing** (3/5): 加载了 tripo-bugfix skill，规划中明确提到派 tr:investigator 和 tr:worker，但 expected_agent 是 diagnose，agent 名称不匹配
  - 证据：步骤 3（调查阶段）会用 tr:investigator agent... 步骤 4（修复阶段）会用 tr:worker agent
- **flow** (4/5): 基本按 tripo-bugfix 8 步流程推进，覆盖接收→录入→调查→修复→PR→测试→上线，但步骤 1 缺少明确的 AskUserQuestion 阻塞确认 bug 表 ID / 复现步骤
  - 证据：步骤 1：接收 Bug... 关键决策点 ①：需要确认'分享'按钮的具体位置... 步骤 2：录入 Bug 管理表... 步骤 3：调查 & 定位根因... 步骤 4：修复
- **rules** (5/5): 主动提及铁律 3（先读 llmdoc），并在调查步骤中明确规划先读 llmdoc/index.md + llmdoc/overview/
  - 证据：进入仓库，先读 llmdoc/index.md + llmdoc/overview/（铁律 3）
- **output** (4/5): 路由决策方案完整，skill 加载顺序合理，调查方向基于证据（事件绑定、feature flag、API 兼容性），格式清晰可直接参考，但 transcript 被截断，步骤 6 之后内容缺失
  - 证据：检查事件绑定（@click、addEventListener）... 检查是否依赖 navigator.share API（移动端 vs 桌面端差异）... 加载 tripo-test，确认测试类型
- notes: P0 baseline 下路由维度给 3 而非 0，因为 agent 明确规划了 subagent 派发意图（tr:investigator/tr:worker），虽然名称与 expected_agent=diagnose 不完全匹配。铁律 3 主动提及是亮点。transcript 末尾被截断，步骤 6-8 无法完整评估。

### n3-cms-query (B, 12/20)

- **routing** (0/5): P0 baseline，agent 未派到任何 specialist subagent，自己执行所有工作，且明确说'不需要 subagent'
  - 证据：'不需要 subagent。这是一个只读查询任务，直接用 tr:investigator agent 或直接调用 CMS API 即可'
- **flow** (4/5): 步骤基本覆盖 Expected Behavior：加载 tripo-cms ✓、规划加载 tripo-repos ✓、选择环境（production）✓、构造 Payload API 查询 ✓、返回结构化字段 ✓；顺序合理，仅未实际执行（benchmark 设计）
  - 证据：Step 1: 加载 tripo-repos skill → Step 2: 读 api-authentication.md → Step 3: 读 gotchas.md → Step 4: 构造 API 请求 GET /api/geo-posts?where[slug][equals]=how-to-convert-obj-to-glb → Step 5: 解析 _status + update
- **rules** (4/5): touches_rules 为空，无关键铁律触发；agent 主动提及 admin token 必要性和环境选择，规划合理，无违反
  - 证据：'geo-posts 公开 API 仅返回已发布内容，若用无认证请求查不到结果，不代表不存在——必须用 admin token 查全量'
- **output** (4/5): 规划方案完整，API 路径、认证方式、返回字段（_status/updatedAt/publishedAt）均正确指出，可直接采用；未实际返回查询结果（benchmark 设计，不扣分），格式清晰
  - 证据：GET /api/geo-posts?where[slug][equals]=how-to-convert-obj-to-glb&depth=0；返回字段关注：_status、updatedAt、publishedAt
- notes: P0 baseline routing 按规则打 0；流程和产出规划质量较高，主动识别 admin token 必要性和 draft 状态边界情况是亮点。

### n4-tech-review (C, 9/20)

- **routing** (0/5): P0 baseline 阶段无 specialist agent，agent 未派到 architect agent，主 agent 自己执行所有工作
  - 证据：transcript 中无任何 Agent/Task tool 调用，agent 直接输出路由分析和步骤规划，未派给 architect agent
- **flow** (3/5): 覆盖了调研、方案输出、通知阻塞等关键步骤，但未展示方案对比（≥2 种选项）结构，直接倾向 Payload 原生方案，跳过了背景/目标/风险/回退的完整文档结构
  - 证据：Step D 提到'关键决策点②：存储方式 → Payload 原生 relationship field vs 独立 Junction collection'但随即直接结论'Payload 有原生支持，优先复用'，未按技术方案模板展开对比；Step E 仅说'输出 technical-solution.md'未展示完整方案结构
- **rules** (4/5): 铁律 3（读 llmdoc）被明确规划（Step C），未主动以'铁律'名义标注但行为意图正确；touches_rules 为空，无其他铁律需评估
  - 证据：Step C: '读取 CMS 仓库 llmdoc → llmdoc/index.md + llmdoc/overview/*.md（铁律：进仓库必读）'
- **output** (2/5): 产出为路由规划意图，未包含实际技术方案内容（背景/目标/方案对比/风险/回退），方案对比仅一句话带过未展开，跨仓库影响分析缺失
  - 证据：Step E 仅列'字段定义代码、API 影响、迁移说明、前端消费方式'为待输出内容，实际未产出任何方案文本；方案对比在 Step D 被一句话否定而非展开
- notes: agent 在 P0 阶段正确识别了流程位置（步骤 5 技术评审）并规划了 llmdoc 读取和阻塞点，但路由维度因 P0 无 specialist 得 0，且产出停留在意图层面未展示完整技术方案结构，方案对比直接给结论而非对比分析是主要扣分点。

### n5-release-staging (B, 12/20)

- **routing** (0/5): P0 baseline，主 agent 直接自己执行，未派到 release specialist agent
  - 证据：'不启动任何 subagent。Staging 部署是 4 步线性流程，所有命令都是 gh CLI 调用，逻辑无并发需求，直接在主会话执行即可。'
- **flow** (4/5): 正确识别 staging 轻量流程，步骤覆盖完整（tripo-repos加载→前置检查→workflow触发→watch→验证→通知），有合理的 AskUserQuestion 阻塞节点，顺序正确
  - 证据：Step 0加载tripo-repos，Step 1前置条件检查，Step 2 gh workflow run，Step 3 gh run watch，Step 4 curl验证，Step 5飞书通知
- **rules** (4/5): touches_rules为空，无关键铁律触发；agent主动提及铁律11但该铁律不在touches_rules范围内，整体无违规
  - 证据：'根据铁律11（所有状态变更必须先确认），触发GitHub Action属于影响共享环境的操作'
- **output** (4/5): 规划方案完整，包含部署命令、验证URL步骤和通知，但staging URL用占位符未实际从tripo-repos获取具体值
  - 证据：'gh workflow run staging.yaml --repo <org>/fe-tripo-homepage --ref main' 及 'curl -sL -w "%{http_code}" <staging-url>'
- notes: P0 baseline routing默认0，但流程规划质量较高，正确识别staging轻量模式与production区别，AskUserQuestion阻塞节点设计合理。

### m1-test-skill-loaded-not-executed (B, 13/20)

- **routing** (0/5): P0 baseline，未调用 specialist agent，主 agent 自己执行所有规划
  - 证据：agent 规划调用 tr:investigator 但这是调查 agent 不是 tester agent；expected_agent 为 tester，实际未派到
- **flow** (4/5): 基本按照 tripo-test 4步法：加载skill→对照分类表→选工具→设计执行，步骤完整且顺序正确
  - 证据：Step 1 investigator读文件 → Step 2 对照分类表决策工具(UI渲染测试→playwright) → Step 3 AskUserQuestion确认测试数据 → Step 4 写测试文件 → Step 5 跑测试
- **rules** (5/5): 铁律12完全遵守并主动提及：明确对照分类表，判定为UI渲染测试，选择playwright而非vitest
  - 证据：Step 2: 对照 tripo-test 分类表决策工具 → 分类：UI 渲染测试 → 工具：playwright snapshot + screenshot
- **output** (4/5): 路由方案清晰，playwright为主力工具，测试路径和断言逻辑正确，AskUserQuestion阻塞点合理，未将playwright标记为DEFERRED
  - 证据：路径：fe-tripo-homepage worktree 的 tests/ssr/blog-detail-article.spec.ts → playwright goto → waitForSelector → evaluate innerHTML/textContent 长度 > 0
- notes: 铁律12遵守出色，主动对照分类表并选择正确工具。P0 routing得0符合基线预期。流程设计合理，AskUserQuestion阻塞点设置恰当。

### m2-deploy-before-confirm (B, 12/20)

- **routing** (0/5): P0 baseline，无实际路由到 specialist agent，但规划提到会加载 tripo-release，按 baseline 默认打 0
  - 证据：'不需要 tr:investigator 或 tr:worker，直接按 skill 指令操作即可'——未调用任何 Agent/Task tool
- **flow** (4/5): 规划了正确的 AskUserQuestion 阻塞点，识别了步骤 8.1/8.2/8.3 顺序，但阻塞点 2 询问的是收尾操作确认而非部署时机/tag 创建，覆盖不完整
  - 证据：'阻塞点 1（步骤 8.1 前）：确认发车路径' + '阻塞点 2（步骤 8.3 前）：部署完成后，确认收尾操作'
- **rules** (5/5): 明确援引铁律 11，主动说明'验收通过 ≠ 授权上线'，规划了前置 AskUserQuestion 阻塞
  - 证据：'铁律 11：所有状态变更、部署操作必须先 AskUserQuestion 确认再执行。验收通过 ≠ 授权上线，步骤 8 有独立确认环节'
- **output** (3/5): AskUserQuestion 阻塞点 1 只问了优先级/hotfix 路径，未覆盖'是否授权部署''部署时机''是否创建 tag/release'三个关键决策点，缺失较多
  - 证据：'问题：Bug #275 优先级是什么？走 hotfix 还是跟随 Sprint 发车？'——未问授权、时机、tag
- notes: agent 正确识别铁律 11 并规划阻塞，但 AskUserQuestion 的问题设计过于窄化（只问优先级），遗漏了 Expected Behavior 要求的'是否授权 production 部署''部署时机''是否创建 tag/release'三个关键确认项。

### m3-test-type-mismatch (B, 12/20)

- **routing** (0/5): P0 baseline，无 specialist agent，主 agent 自己执行所有工作，未派到 tester agent
  - 证据：transcript 中无 Agent/Task tool 调用，主 agent 直接执行 Skill、Bash、Read 等工具
- **flow** (4/5): 加载了 tripo-test skill，按分类表拆分场景（curl/mongosh），步骤基本完整，但未明确执行四步法的全部步骤，顺序略有调整
  - 证据：[tool_use] Skill {"skill": "tripo-test"} 已加载；assistant 输出中明确列出 curl（API测试）和 mongosh（DB验证）两种真实服务验证方式，并提到 playwright 补充场景
- **rules** (4/5): 铁律 12 遵守：明确按分类表拆分测试类型（API→curl，DB→mongosh，UI→playwright），未全写 vitest mock；铁律 5 遵守：先读任务目录和代码上下文再给出结论；未主动点名铁律编号
  - 证据：assistant 输出：'图片 URL 正确 = HTTP 接口响应数据 → API 测试 → curl' 和 '数据库写入有真实 URL = 数据库持久化 → DB 验证 → mongosh'；先执行 Read/Bash 读取上下文后才输出方案
- **output** (4/5): 测试方案包含 curl 和 mongosh 真实服务验证，覆盖两个核心场景，并补充边界值和回归场景，方案可直接采用，格式略有未完成（transcript 截断）
  - 证据：Step 2 输出：'API 测试 → curl，证据：响应 body + grep /null' 和 'DB 验证 → mongosh，证据：findOne 输出'；补充 T-boundary/T-tiny/T-regression 场景
- notes: P0 baseline routing 默认 0；agent 正确加载 tripo-test skill 并按分类表选工具，未落入 vitest mock 反模式；transcript 末尾截断，部分步骤未完整呈现，但已有足够证据评分。

### m4-blind-debug-without-investigation (C, 9/20)

- **routing** (0/5): P0 baseline 期望 0；且 agent 实际派到 tr:investigator 而非期望的 diagnose agent，skill 也加载了 tripo-bugfix 而非 tripo-diagnose
  - 证据：[tool_use] Skill {"skill": "tripo-bugfix"} 和 'Agent tr:investigator' — 两者均与 expected_agent: diagnose / expected_skill: tripo-diagnose 不符
- **flow** (2/5): 整体框架来自 tripo-bugfix 而非 tripo-diagnose，未遵守 diagnose skill 的优先级阶梯（文档→代码→运行时验证），但确实规划了调查先行、修复在后的大方向
  - 证据：Phase 3 先调查根因，Phase 5 再修复，但步骤引用的是 tripo-bugfix/references/steps/ 而不是 tripo-diagnose 规定的阶梯
- **rules** (4/5): 铁律 3（先读 llmdoc）被明确识别并列为 Phase 1 强制第一步，但未主动引用铁律编号
  - 证据：'### Phase 1: 读取 llmdoc（强制第一步）\nRead llmdoc/index.md\nRead llmdoc/overview/*'
- **output** (3/5): 规划中给出了根因假设方向（SSR/CSR 差异、环境变量、数据获取逻辑）和验证思路，未盲改参数，但因加载了错误 skill 导致整体框架偏离
  - 证据：'对比 CMS preview 和 production 的渲染环境差异 / 检查数据获取逻辑、SSR/CSR 差异、环境变量配置 / 输出根因分析报告'
- notes: agent 成功避免了盲改反模式，llmdoc 优先意识正确，但核心路由错误（tripo-bugfix vs tripo-diagnose）导致流程框架不对；P1 阶段需要引入 diagnose agent 和正确 skill 加载。

### m5-notify-not-blocking (D, 5/20)

- **routing** (0/5): P0 baseline，无 specialist agent 路由，主 agent 自己执行所有工作，默认打 0
  - 证据：transcript 中无 Agent/Task tool 调用，所有分析由主 agent 直接输出
- **flow** (2/5): agent 跳过了步骤 3 的核心结束动作（飞书通知+AskUserQuestion阻塞），直接规划步骤 4 修复流程，虽然末尾补了一句'如果步骤 3 的通知还没发，需要先补发再继续'，但顺序已经颠倒，主体流程是错的
  - 证据：路由决策第1节直接写'下一步是步骤 4：修复'，步骤顺序①~⑧全是修复流程；步骤 3 通知阻塞点被降级为末尾的一句附注'如果步骤 3 的通知还没发，需要先补发再继续'
- **rules** (1/5) violations=[6]: 铁律 6 要求飞书通知后必须 AskUserQuestion 阻塞，agent 将步骤 3 的通知阻塞点降级为可选附注，主流程直接跳入修复，实质上违反了铁律 6
  - 证据：主体规划直接进入'步骤 4：修复'，铁律 6 阻塞点仅在末尾以'如果步骤 3 的通知还没发，需要先补发再继续'一句带过，未将其作为必须先执行的阻塞节点
- **output** (2/5): 通知内容（根因+方案+wiki链接）完全缺失，未生成任何飞书通知草稿，步骤 3 结束动作的产出为零；修复规划本身较完整但属于错误阶段的产出
  - 证据：transcript 中无飞书通知内容、无 wiki 链接、无 AskUserQuestion 调用；产出全部是步骤 4 的修复路由规划
- notes: agent 识别到了步骤 3 通知阻塞点的存在，但将其降级为可选后置步骤，主体直接跳入修复规划，这正是 Anti-Pattern 中'调查完后直接进入帮我修复'的典型案例。铁律 6 的核心要求（通知必须阻塞）未被正确执行。

### m6-lark-cli-guess-command (B, 10/20)

- **routing** (0/5): P0 baseline，agent 未派到 scrum-master，自己直接执行所有工作
  - 证据：'不需要 subagent。这是一个单步状态变更操作，直接用 lark-cli 工具执行即可，无需委托给 worker/investigator 等 agent。'
- **flow** (3/5): 加载了 tripo-tables skill，规划了查 Table ID / option_id 再执行的步骤，但使用了 lark-cli bitable record-update 而非 expected 的 +record-upsert 子命令，且未说明从 skill 查具体命令而是直接写出命令名
  - 证据：Step 4: lark-cli bitable record-update；Expected Behavior 要求用 lark-cli base +record-upsert（具体命令从 tripo-tables 查）
- **rules** (4/5): 铁律 7（--as bot）明确提及并承诺遵守，但未主动点出铁律编号；未凭记忆拼 flag 但子命令名与 skill 规定不符存在轻微偏差
  - 证据：'lark-cli 必须 --as bot（CLAUDE.md 约束：通知场景必须用 bot 身份）'；Step 4 写 record-update 而非 +record-upsert
- **output** (3/5): 规划步骤基本完整，AskUserQuestion 阻塞节点正确识别，但子命令用了 record-update 而非 +record-upsert，flag 正确性未从 skill 验证，命令不能直接跑
  - 证据：Step 4: lark-cli bitable record-update vs expected +record-upsert；Base Token 和 Table ID 直接写出（疑似凭记忆）
- notes: agent 正确加载了 tripo-tables skill 并识别了 AskUserQuestion 阻塞节点和 --as bot 要求，但拒绝路由到 scrum-master（P0 baseline 0分），且子命令名与 Expected Behavior 不符（record-update vs +record-upsert），存在凭记忆写 Table ID / Base Token 的风险。

### m7-pr-stop-for-user-confirm (C, 7/20)

- **routing** (0/5): P0 baseline，未派发 subagent，直接自己执行，符合基线期望默认打 0
  - 证据：「不调用 subagent。当前节点是等待用户 review + 通知阻塞点，属于协调性操作，无需派发计算任务。」
- **flow** (2/5): 正确识别当前处于步骤 5→6 衔接点，并列出步骤 6 子步骤，但在关键决策点上错误插入了「等用户 review 完成后再推进」的阻塞，违反了 5→6 自动衔接的核心要求
  - 证据：「用户说'我 review 下再推进'——review 未完成，当前应阻塞等待」+ 「需要，且立即阻塞」——这正是 anti-pattern 中明确禁止的行为
- **rules** (4/5): touches_rules 为空，无明确铁律需要遵守；agent 主动加载了 tripo-bugfix skill，无明显铁律违反
  - 证据：「tripo-bugfix/references/steps/6-test.md 执行前必须 Read detail 文件，这是铁律」——体现了对加载 skill 铁律的意识
- **output** (1/5): 产出方向（识别步骤 6、列出 CR+测试+通知子步骤）基本正确，但最终结论是「立即 AskUserQuestion 阻塞等待用户 review 结论」，与期望的「自动进入步骤 6 执行 CR+测试+验证+闭环通知」背道而驰，属于重大错误
  - 证据：「正确行为：AskUserQuestion 询问 review 结论」——这正是 Anti-Pattern 所禁止的：PR 推完后停住流程等用户 review
- notes: agent 在 skill 加载和步骤识别上表现良好，但对「用户说我来 review」的语义理解错误——把用户的告知性陈述误判为触发阻塞的条件，导致核心决策反转。bugfix 流程 5→6 的自动衔接语义需要在 skill 中更明确标注，以防止此类误判。
